> felhő alapú projekt kezelése
> felhasználói felület készítése
> korábbi generálások tárolása (pl 3 db)
> generált teszt utólagos szerkesztése, hogy a felhasználó kiegészíthesse azt (pl displayName, ellenőrzött adatok felülírása)
> opc. automatikusan induljon minden


> Összetett paramétereket (pl. List<String>, Map<K, V>, Optional<T>, tömbök).
> Vezérlési szerkezeteket (if-else, ciklusok, try-catch).
> API hívásokat (pl. cloud szolgáltatások, adatbázis-lekérdezések).
> Komplex visszatérési értékeket (pl. List<Integer>, Future<T>).
> Statikus és példány metódusokat.
> Konstruktorokat paraméterekkel.