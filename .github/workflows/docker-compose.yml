version: '3.8'

services:
  app:
    image: viku243/unittestgen:latest
    container_name: language2tests
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ENV=prod